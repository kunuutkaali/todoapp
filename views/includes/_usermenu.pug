header 
    nav 
        .col-left 
            a(href="/todos") 
                img(src="/img/logosmall.svg", alt="Todos")
            a(href="/users")=locals.user
                if(locals.user) 
                    #{locals.user}
                    
        
        a(href="/todos/new", class="cta") New todo
        a(href="/users/logout", class="cta") Log out